type Query {
    positions: [TrainPosition]
    departures(station: String!): [Departure!]
    departure(id: String!): Departure!

    stations: [Station]
    station(code: String!): Station
    stationSearch(query: String!): [Station!]

    journey(id: String!): Journey!
}
